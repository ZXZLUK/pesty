# RISKS.md — 冻结源码风险账本

基准：`70fd6b2c47d48ce03932e7e43b930f9ee26fcec7`

严重度：

- `P0`：可能执行错误动作、泄露敏感信息或造成不可逆数据损失；
- `P1`：核心能力可确定性失效；
- `P2`：边界条件、性能或可维护性风险；
- `P3`：已有缓解，但重构时需保护。

| ID | 风险 | 级别 | 发布状态 |
| --- | --- | --- | --- |
| R1 | 敏感剪贴板捕获与明文落盘 | P0 | 阻断 |
| R2 | 大对象与主线程全量处理 | P1 | 阻断规模化使用 |
| R3 | store 解码失败后数据永久丢失 | P0 | 阻断 |
| R4 | Accessibility 授权与重命名失效 | P1 | 阻断直粘发布 |
| R5 | 多显示器回归 | P2 | 需本机验证 |
| R6 | Secure Input 静默吞事件 | P1 | 需降级策略 |
| R7 | MAS/CloudKit 资产不属于 fork | P1 | 阻断 MAS |
| R8 | 双构建形态与维护分叉 | P2 | 需方向决策 |
| R9 | 热键注册恢复状态机失效 | P1 | 阻断 |
| R10 | 缺失图片导致粘贴旧剪贴板 | P0 | 阻断 |
| R11 | iCloud Drive 删除复活与 metadata 分叉 | P0 | 阻断可靠同步声明 |
| R12 | 保存失败仍发送成功通知 | P1 | 阻断同步可信性 |
| R13 | 文件监听永久丢失 | P1 | 阻断可靠同步声明 |
| R14 | 内容身份函数不一致 | P1 | 阻断同步/去重重构 |
| R15 | 目标聚焦超时与粘贴结果不可观测 | P1 | 阻断“可靠直粘”声明 |

## R1. 敏感剪贴板捕获与明文落盘 — P0

现状：

- 过滤 Concealed / Transient / 可选 AutoGenerated 类型；
- 支持按 source bundle ID 排除；
- history 与 RTF 明文保存在 `store.json`；
- 图片明文保存在 `images/`；
- iCloud Drive 模式把相同明文文件置于 CloudDocs。

失效条件：

- 来源 App 不写约定隐藏类型；
- 来源识别因 0.4s 轮询发生漂移；
- 用户未手动排除密码管理器、银行或通信 App。

影响：

- 密码、OTP、API key、私人文本可能长期保存并同步。

最低修复：

- 默认敏感 App denylist；
- 明确“暂停捕获”可见状态；
- 单条/按 App retention；
- 可选本地加密；
- 同步前重新做隐私策略评估。

## R2. 大对象、全量 JSON 与搜索分配 — P1

失效条件：

- 复制数十 MB 文本、RTF 或 TIFF；
- history 接近 5000；
- 每击键重新构建全量 `searchableText`；
- 主线程执行编码、过滤和图像转换。

影响：

- 卡顿、内存峰值、写放大、UI 无响应。

最低修复：

- 捕获侧硬尺寸上限；
- 后台序列化；
- 搜索字段缓存或索引；
- 图片缩略图缓存；
- 增量存储。

## R3. 解码失败放大为永久数据丢失 — P0

失败路径：

```text
store.json schema drift/corrupt
→ load() 使用 try? 返回空
→ App 以空 history 启动
→ 任一状态变化触发 save
→ 空 Snapshot 原子覆盖旧文件
```

最低修复：

- 解码失败时 quarantine 原文件；
- 阻止自动覆盖；
- 暴露用户可见恢复入口；
- 保留轮转备份；
- 为旧 schema 实现宽松迁移。

## R4. Accessibility 与代码身份 — P1

失败条件：

- 改 bundle ID；
- designated requirement 不稳定；
- 用户拒绝或撤销辅助功能权限；
- 正式安装与开发构建身份不同。

影响：

- 直粘失效，且当前降级反馈不足。

最低修复：

- 新品牌按全新安装处理；
- 稳定 signing identity；
- 明确“仅复制”降级；
- 正式安装手动授权 smoke。

## R5. 多显示器 — P2

当前代码包含针对历史 issue 的保护逻辑，但没有本轮真实多屏证据。重构 `BarWindowController` 前必须建立至少：

- 横向双屏；
- 纵向叠屏；
- 鼠标位于边界；
- 拔插显示器；
- 分辨率变化；
- 全屏 Space。

## R6. Secure Input — P1

合成 `⌘V` 可能被系统静默吞掉。

最低修复：

- 检测 secure input；
- 回退“仅复制 + 激活”；
- 给出可感知但不泄露内容的状态提示；
- 禁止尝试绕过系统保护。

## R7. MAS / CloudKit 所有权 — P1

原 team、container、profile 与签名身份不属于 fork。没有自有 Apple Developer 资产前：

- MAS 构建不可验收；
- CloudKit 容器不可复用；
- 上游 iOS wire contract 不属于本仓库的可控发布面。

建议：近期只维护直装版，冻结 MAS 分支。

## R8. 双产品形态 — P2

`#if MAS` 同时分叉粘贴、同步、设置、权限和通知。任何共享逻辑改动都需要双矩阵验证。若没有明确 MAS 路线，应删减维护面，而不是长期保留不可验证代码。

## R9. 热键恢复状态机失效 — P1

失败路径 A：

```text
记录 previous EventHotKeyRef
→ unregister(previous)
→ 新组合注册失败
→ 把已失效 previous ref 重新赋给变量
→ 实际没有热键
```

失败路径 B：

```text
用户录入 Command+A
→ keyCode = 0
→ reload 先注销旧组合
→ guard keyCode != 0 直接返回
→ 热键永久丢失
```

失败路径 C：

```text
配置 A 注册失败并排队 retry
→ 用户改成配置 B，B 注册成功
→ A 的旧 retry 醒来
→ 注销 B
→ 尝试旧配置 A
```

最低修复：

- 存储 active keyCode/modifiers，而非复用失效 ref；
- keyCode 0 作为合法值；
- generation token 取消过期 retry；
- 把 Carbon 注册器抽象为可注入接口并写状态机测试。

## R10. 缺失图片导致粘贴旧剪贴板 — P0

失败路径：

```text
ClipItem.type == image
→ 对应 PNG 不存在/不可解码
→ copy() 不修改 pasteboard，只返回旧 changeCount
→ paste() 继续激活目标并发出 ⌘V
→ 目标收到此前的剪贴板内容
```

影响：执行错误粘贴，可能泄露旧敏感内容。

最低修复：

- `copy` 返回显式 success/failure；
- failure 时禁止激活/发送 ⌘V；
- 用户获得无内容细节的失败反馈；
- 使用 unique pasteboard + injectable image loader 写回归测试。

## R11. iCloud Drive 删除复活与 metadata 分叉 — P0

失败路径：

```text
Mac A 删除 X 并保存
Mac B 仍持有 X
Mac B 读取 A 的文件
mergeExternal = local ∪ remote
X 被保留并再次写回
```

同类问题覆盖：

- clear history；
- Pinboard item 删除；
- Pinboard 删除；
- Pinboard name/color 更新冲突。

最低修复路径二选一：

1. 在自有版本中禁用/标记 experimental；
2. 引入 per-record version + tombstone + deterministic conflict rule。

仅靠 whole-file union merge 无法满足可靠删除语义。

## R12. 保存失败仍发送成功通知 — P1

`saveNow()` 对写入使用 `try?`，随后无条件发送 `.pestyStoreDidSave`。

影响：

- CloudKit diff 可能基于未落盘状态；
- UI/同步层无法区分成功与失败；
- 调试日志形成假阳性。

最低修复：

- `saveNow() -> Result`；
- 仅在磁盘提交成功后发 saved event；
- 独立 failure event / observable error；
- 注入不可写目录测试。

## R13. 文件监听永久丢失 — P1

`startWatching()` 对 `open(storeURL, O_EVTONLY)` 失败直接返回。文件被删除、改名、iCloud 驱逐或冲突处理期间，监听可能永久停止。

最低修复：

- 监视父目录而不是单文件；
- 文件重新出现后重建 watch；
- 有上限退避和可观测状态；
- 进程内故障注入测试。

## R14. 内容身份函数不一致 — P1

`sameContent` 类型敏感；`contentKey` 把 text/richText/link 合并。

影响：

- 捕获允许并存；
- 编辑或同步又可能删除其一；
- 测试冻结的行为与同步行为互相矛盾。

最低修复：

- 定义一个唯一 `ContentIdentity`；
- 全部路径调用同一函数；
- 覆盖 text/link/richText、RTF 格式差异、URL normalization 和 file order。

## R15. 目标聚焦超时与结果不可观测 — P1

目标 App 激活后只轮询约 600ms。超时后不发送事件，也没有失败回调。即使发送事件，也无法确认目标实际接收。

最低修复：

- paste transaction 状态；
- focus timeout 明确失败；
- secure input 降级；
- 仅在可确认条件下播放成功音；
- 真实 TextEdit/浏览器/Terminal smoke。

## 停止规则

在 R3、R9、R10、R12、R14 未修复前，不发布自有版本。  
在 R11、R13 未修复前，不宣称可靠多设备同步。  
在 R4、R6、R15 未完成正式安装 smoke 前，不宣称可靠直接粘贴。
