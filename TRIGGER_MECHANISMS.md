# 触发与自动化机制手册（TRIGGER_MECHANISMS.md）

> 本文档沉淀 ClipBar / QuickNote 两个边缘应用所有"触发与自动化"机制的
> **解决什么问题、实现要点、关键常量、已知代价**。新增任何触发类功能前，
> 先读这里：能复用的机制直接复用，避免重新踩一遍已经付过学费的坑。
>
> 维护约定：每新增或修改一个机制，同步更新对应小节；被新方案替代的旧机制
> 保留条目并标注"已被 XX 替代"，作为设计决策记录。

## 1. 顶边热区召唤（ClipBar）

- **解决什么**：免快捷键、免菜单，鼠标碰屏幕顶边中部立即召唤剪贴板面板。
- **实现要点**：0.1 秒光标位置轮询（`NSEvent.mouseLocation`），判定"顶边
  4pt 带 × 中间 50% 宽度"区域；进入区域立即 `showBar()`。
- **关键常量**：`bandThickness=4`、`leftMarginRatio=0.2`、
  `rightMarginRatio=0.3`、`pollInterval=0.1s`、停留 0 秒。
- **已知代价与边界**：顶边中段瞬时触发（路过即弹，用户已接受）；面板为
  非激活面板（`.nonactivatingPanel`）+ 不调用 `NSApp.activate`——召唤不抢
  "活跃应用"身份，其他软件的浮层（如右侧设置栏）不会被收起。
- **历史演进**：隐形窗口触发条（会吞掉顶边点击）→ 分区 → 光标轮询
  （彻底消除点击干扰）。停留时长 0.25s→0.2s→0s（用户逐轮实测后锁定 0）。

## 2. 底边热区唤出（QuickNote）

- **解决什么**：碰屏幕底边中部（避开 Dock 图标带），停 0.3 秒弹出灵感
  输入面板。
- **实现要点**：与顶边同款光标轮询；中间 20% 宽度、底边 4pt 带；下穿入带
  启动 0.3 秒停留计时，上移出带取消并重新武装。
- **已知代价与边界**：底边中段停留 0.3 秒即弹（用户已接受）；面板会取
  焦点（打字面板，设计本意）。

## 3. 下半部自动收起（ClipBar）

- **解决什么**：面板弹出后用户把鼠标移到屏幕下半部 = "我去忙别的了"，
  面板自动收起，无需任何按键或点击。
- **实现要点**：同一个 0.1 秒轮询；鼠标 y 坐标下穿面板所在屏幕的中线
  （下边界不低于面板底缘）即 `hideBar()`；**上穿中线重新武装**——召唤时
  鼠标已在下半部则不武装，防止召唤瞬间自灭。
- **已知代价与边界**：面板内部移动永不触发（自身窗口事件不进全局监听）。

## 4. 外部点击收起（两应用通用）

- **解决什么**：点击面板外任意处收起，且**绝不影响其他软件**。
- **实现要点**：全局 mouseDown 监听（仅面板存活期间安装）+ 判定落点是否
  在面板 frame 内；**用 `NSApp.windows` 内存扫描判定"是否点在自己的
  浮层上"**（菜单弹层、设置窗、预览窗）——自己的浮层不算外部。
- **已弃用方案（勿回退）**：`CGWindowListCopyWindowInfo` 判定——跨进程
  IPC，每次收起增加几十至上百毫秒延迟；`windowDidResignKey` 失焦收起——
  agent 类应用会定时回收焦点，导致面板被误收（Hermes 实测）。

## 5. 链接规则引擎 + 幂等

- **解决什么**：捕获到特定域名链接时自动执行动作（打开浏览器 / 运行内联
  zsh 脚本，链接为 `$1`），规则可在设置里增删改、暂停、切换动作。
- **实现要点**：规则为 JSON 数据存储；域名后缀式匹配（覆盖子域）；在
  `addCaptured` 两条路径（新入库 / 重复提升）末尾调用引擎。
- **幂等（关键）**：**与头条完全相同的即时重复捕获不触发规则**——语音
  输入软件会故意回拷剪贴板，不幂等会导致同一篇文章反复弹浏览器。仅当
  中间隔了其他内容后再出现才视为新事件。
- **可观测性**：`CLIPBAR_RULE_DEBUG=1` 时触发写入
  `/tmp/clipbar_rule_fires.log`。

## 6. 抓取全文序列（openCopyAllClose 动作）

- **解决什么**：把浏览器里打开的文章全文自动收进剪贴板并清理标签页。
- **实现要点**：打开链接 → 等 2.5 秒渲染 → ⌘A → +0.1s ⌘C → +0.1s ⌘W，
  全部发往前台应用；**每一步发出前校验默认浏览器仍在前台**，否则放弃
  整个序列——合成 ⌘W 打进别的应用会误关别人的标签。
- **已知代价与边界**：2.5 秒渲染等待对超长/慢网文章可能不足（抓到半截，
  表现为全文条目中段缺失——回调此常量即可）；序列期间用户切走应用则
  自动放弃。

## 7. 输入法（按应用切换的系统行为）

- **现象**：面板成为 key window 时，菜单栏输入法图标切到本应用记住的
  源（通常 ABC）；回到之前的应用自动恢复——这是 macOS 按应用记忆输入源
  的系统行为，确定性、非 bug。
- **为什么顺其自然**：面板打开期间的 ABC 状态正好支持拼音字母直输搜索；
  macOS 26.5 SDK 已将 TIS 切换 API 从 Swift 可见头文件移除，代码层无法
  干预（已验证并回退）。
- **用户侧可选**：系统设置 → 键盘 → 输入法 → 关闭"自动切换到文稿的
  输入源"，可全局停用按应用切换。

## 8. 事件监听线程纪律（CGEventTap）

- **解决什么**：全局热键（⌘⇧N 等）需要挂在系统事件流上。
- **实现要点**：listen-only tap 的 source 必须跑在**独立后台线程**的
  独立 runloop 上——挂在主线程时，回调排队等主线程会**延迟全系统输入
  投递**（表现为点击手感发涩）；正常退出时 `tapEnable(false)` 注销。
- **已知代价**：进程被 SIGKILL 时监听来不及注销，系统短暂等待死监听器
  后自动回收（短暂输入发涩，自动恢复）。

## 9. Clipboard Agent → 本地内容编译器

- **解决什么**：让 ClipBar 从“保存剪贴板”升级为可控的内容编译入口。顶栏右侧
  直接显示 `Podcast Agent` 总开关；开启后显示当前编译形态菜单。
- **编译形态**：v1 固定四种稳定 ID：`quick`（快速理解）、`spoken`（口播·平实）、
  `humorous`（口播·轻幽默）、`judgments`（20 条判断）。选择会持久化，并通过 `$2`
  传给 `pi-podcast-compiler`，不是只改变 UI 文案。
- **语义路由 v1**：producer 可把严格 marker
  `[[CLIPBAR:v1;source=youtube;kind=transcript;intent=compile]]` 放在复制文本第一行。
  ClipBar 捕获后先解析并剥掉 marker，再把干净正文存入历史并送给编译器；marker 不进入模型。
- **结构 fallback**：没有显式 marker 时，至少 2 行独占时间戳（如 `0:07`）的字幕仍会触发。
  普通长文本不再因为超过某个字数就自动调用模型，避免邮件、代码、网页长段落误付费。
- **下一层协议**：代码中保留了 Chromium Web Custom Format 的严格映射解析器，供未来把
  metadata 从正文中完全分离；当前运行时 v1 以首行 marker 为确定性接口。
- **幂等**：仍挂在“新捕获”路径；与头条完全相同的紧邻重复沿用现有 dedup 跳过，
  不会重复触发 Agent。
- **默认桥接**：`subtitleScript` key 为兼容历史继续保留，但默认脚本现在是
  `printf '%s' "$1" | .../clip-compile.mjs --stdin --preset "$2"`。旧默认 bridge 会在启动时
  自动迁移；用户自定义脚本不覆盖。`subtitleTriggerEnabled` 同样作为兼容 key 保留，
  语义已升级为 Agent 总开关。
- **后台纪律**：只用 `Process.run()` 启动 zsh，不等待模型完成、不抢焦点、不合成鼠标事件。
  编译器成功拿到正文后才 `pbcopy` 回写并发通知。
- **输出回环防护**：编译器在 `pbcopy` 前以结果正文的 SHA-256 写入 one-shot 临时 marker；
  `AgentTrigger` 捕获到同一哈希时消费 marker，只保存结果、不再次调用模型。marker 文件名只有哈希，
  不保存正文；按哈希分文件，因此并发任务不会互相覆盖。
- **失败可观测**：脚本 stdout/stderr 继续丢弃以免污染前台；非零退出由
  `terminationHandler` 写系统日志。失败不得覆盖 owner 当前剪贴板。

## 10. 通用设计原则（从以上机制提炼）

1. **零遮挡**：任何触发机制不得以隐形窗口形式覆盖用户界面——用光标
   轮询/事件监听替代覆盖窗口，点击永不被吞。
2. **零抢焦点**：召回型面板（查看/点击）不激活应用；打字型面板
   （QuickNote）显式激活——两者不可混淆。
3. **前台门禁**：一切合成按键发出前校验前台应用身份，宁可放弃不可误发。
4. **幂等**：重复事件（同内容、同来源）不得重复执行有副作用的动作。
5. **失败有声**：所有静默回退必须留痕（日志或状态），禁止无记录降级。
6. **分区可调**：热区边界、停留时长全部为命名常量，随用户反馈一轮一调。
