# ARCHITECTURE.md — 架构地图

> 基准：momenbasel/pesty @ `70fd6b2c47d48ce03932e7e43b930f9ee26fcec7`（v1.2.0 之后，2026-08-12）
> 本文档只描述现状，不含改动建议。风险见 RISKS.md，重命名清单见 RENAME.md。

## 总览

- 纯原生 macOS 应用：Swift 6 工具链编译、语言模式 v5、SwiftUI + AppKit 混合、Carbon 全局热键。
- Swift Package 单 executable target（`Pesty`），**零第三方依赖**，只用系统框架：AppKit、SwiftUI、Carbon.HIToolbox、CloudKit（仅 MAS 构建）、CryptoKit、ServiceManagement、UniformTypeIdentifiers、Observation。
- 菜单栏常驻应用（`LSUIElement=true`，`.accessory` 激活策略），主界面是从屏幕底部滑出的全宽条状 NSPanel。
- 通过条件编译 `#if MAS` 分叉为两个产品形态：直装版（Homebrew/DMG，无沙盒，直接粘贴）与 Mac App Store 版（沙盒，CloudKit 同步，不直接粘贴）。

```
Main.swift (@main PestyMain)
  └─ AppController (NSApplicationDelegate, 单例, @MainActor)
       ├─ ClipboardMonitor      剪贴板轮询捕获
       ├─ ClipboardStore        历史/Pinboard/持久化/选择状态 (单例, @Observable)
       │    └─ CloudSyncService (仅 #if MAS) CKSyncEngine 同步
       ├─ HotKeyCenter          Carbon 全局热键 (单例)
       ├─ BarWindowController   底部滑动面板 (NSPanel + SwiftUI BarView)
       ├─ PasteService          回写剪贴板 + 合成 ⌘V (enum, 无状态)
       └─ Settings              UserDefaults 包装 (单例, @Observable)
```

## 1. App lifecycle

`Main.swift:4` — `@main struct PestyMain`：手工创建 `NSApplication`，挂 `AppController.shared` 为 delegate 后 `app.run()`（不走 SwiftUI App 协议）。

`AppController.swift`：
- 启动（`applicationDidFinishLaunching`）：设 `.accessory` 策略 → 装配主菜单（App/Edit/Window，为文本编辑器与快捷键响应链服务）→ 监听 `NSWorkspace.didActivateApplicationNotification`（记录 `lastActiveApp`）、`didWakeNotification`（睡眠唤醒：强藏面板 + 重注册热键）、`didChangeScreenParametersNotification`（显示器变化：只强藏面板，**不**重注册热键，避免显示 churn 期间注册失败导致热键永久丢失）→ 启动 monitor 与热键 → 状态栏图标/登录自启（SMAppService）→ MAS 构建注册远程通知并可能启动 CloudKit。
- `--demo` 参数：`store.seedDemo()` 注入演示数据并弹出面板（`Util/DemoSeed.swift`）。
- 首次启动：延迟 0.6s 打开 Settings（onboarding）。
- `applicationShouldHandleReopen`：再打开 App 是"逃生舱"——若状态栏图标被隐藏则强制恢复并打开 Settings（否则无热键+无图标=彻底失联）；否则显示面板。
- `applicationWillTerminate`：`store.saveNow()` 落盘。
- `AppController.restart()`：`open -n` 自身 + terminate（Accessibility 授权后重启用）。
- 粘贴目标解析 `pasteTarget`：`[lastActiveApp, previousApp, frontmostApplication]` 依次取第一个存活且非自身进程；`isPesty()` 按 PID/bundleID 判定自身。
- 面板内键盘路由：`NSEvent.addLocalMonitorForEvents(.keyDown)`（`startKeyMonitor`，仅面板显示期间安装），`handleKey` 只处理发到面板窗口的事件（编辑器/警告框/Settings 的事件放行）。详见"全局热键"节。

## 2. ClipboardMonitor（捕获）

`Monitor/ClipboardMonitor.swift`，`@MainActor` class，**轮询模型**：

- `Timer` 0.4s 间隔在 `.common` mode 轮询 `NSPasteboard.general.changeCount`；变化才继续。无 `NSPasteboard` 通知依赖（macOS 无此类通知，轮询是常规做法）。
- `suppressUntilChangeCount`：自己回写剪贴板后（复制/粘贴动作）记录 changeCount，避免把自己的写入再捕获一遍。
- `isPaused`：用户暂停（⌘⇧P / 菜单），暂停期间 changeCount 仍更新基准，恢复后不补捕。
- 过滤（`makeItem`）：
  - `org.nspasteboard.ConcealedType`（密码管理器标记）→ 丢弃；
  - `org.nspasteboard.TransientType` → 丢弃；
  - `Settings.ignoreConcealed`（默认开）时 `org.nspasteboard.AutoGeneratedType` → 丢弃。
- 来源归属：优先读剪贴板自声明类型 `org.nspasteboard.source`（做合法性校验：非空、≤255、仅 `[A-Za-z0-9.-]`，防投毒）；否则用当前 frontmost app；frontmost 是自己或为空时退回 `AppController.lastActiveApp`（轮询延迟期间用户可能已切换 App）。
- 忽略列表匹配三处来源（declared/src/lastActive）任一命中即丢弃——注释明言"漏报比误报代价大（密码会进历史和 iCloud）"。
- 类型提取优先级：file URLs（仅 `file://`）→ 图片（png 或 tiff，tiff 转 png，SHA256 去重哈希，文件写入 images 目录后只存文件名）→ 文本（有 RTF 数据 → `.richText`；单行 http(s) URL → `.link`；否则 `.text`）→ 颜色。全不匹配返回 nil。
- 捕获结果交给 `ClipboardStore.shared.addCaptured`。

## 3. ClipboardStore（状态与持久化）

`Store/ClipboardStore.swift`（750 行，最大文件），`@Observable @MainActor` 单例：

- 数据：`history: [ClipItem]`（新在前）、`pinboards: [Pinboard]`。全部常驻内存。
- UI 状态也在这里：`source`（history 或某 pinboard）、`searchText`、`selectedID`、多选集合 `multiSelectedIDs` + 锚点、`barPresentationToken`（每次打开面板自增，让视图重置滚动位置）。
- `visibleItems`：按 `source` 取集合 + 按 `searchableText` 小写包含过滤（每次访问全量计算）。
- 去重 `contentKey`：`img:sha256` / `col:hex` / `file:urls` / `txt:text`。重复捕获提升到队首（更新 createdAt）而非插入副本；编辑内容也会清除同容器内旧副本。
- 保留策略 `trimHistory`：两种模式——条数（`historyLimit`，默认 500，UI 上限 5000）或时间（`historyRetentionDays`，另有硬性 `timeRetentionSafetyCap = 5000` 兜底）。被裁剪条目的 UUID 记入 `retentionPrunedRecordNames`，MAS 构建据此**不**把本地裁剪当删除同步到云端。
- 删除语义：只删当前容器（历史删除不动 Pinboard 存档，反之亦然）；图片文件引用计数式清理（`deleteImageFile` 检查是否仍被引用）。

### persistence（文件布局，无数据库）

```
~/Library/Application Support/Pesty/        （目录 0700）
  store.json                                （0600，原子写）
  images/<UUID>.png                         （0600）
  ck-tmp/                                   （仅 MAS，CloudKit 资产暂存）
  cksync-state.json / cksync-shadow.json    （仅 MAS）
  ck-system-fields/<uuid>.ckrecord          （仅 MAS）

开启 iCloud Drive 同步（非 MAS）时改为：
~/Library/Mobile Documents/com~apple~CloudDocs/Pesty/  （同布局）
```

- `Snapshot { history, pinboards }` 整体 JSON 编解码；保存 0.4s 防抖（`scheduleSave`）+ 退出时 `saveNow`；写入 `Data.write(options: .atomic)` 后补设 0600。
- 加载失败（解码抛错）静默返回空历史——见 RISKS.md R3。
- 图片按需从磁盘加载（`loadImage` → `NSImage(contentsOf:)`），无内存缓存。
- UserDefaults 键（`Settings/Settings.swift` 的 `Keys` 枚举）：historyLimit、historyRetentionMode、historyRetentionDays、hotkeyKeyCode、hotkeyModifiers、quickPasteModifier、plainTextModifier、launchAtLogin、hideOnClickOutside、pasteDirectly、playSound、ignoreConcealed、ignoredSourceAppBundleIDs、barHeight、showMenuBarIcon、onboarded、iCloudSync、cloudKitSync，另有 `SourceColor` 用的 `appColorMap`（bundleID → 调色板索引，保证每 App 卡片头色稳定）。

## 4. PasteService（回写与直接粘贴）

`Monitor/PasteService.swift`，无状态 enum：

- `copy(item)`：按类型重建剪贴板内容（file: URLs+文本兜底；color: NSColor+hex 文本；richText: RTF data+纯文本；image: 从磁盘加载 NSImage），并写入 `org.nspasteboard.source` 标记（保留原 App 归属，来源缺失时用 `packagedBundleID = "com.greycorelabs.pesty"` 兜底）。返回 changeCount 供 monitor 抑制自捕获。
- `paste(item, into:)`：
  - MAS 构建：仅复制 + `target.activate()` 把焦点还给原 App，用户手动 ⌘V（沙盒无 Accessibility）。
  - 直装构建：`Settings.pasteDirectly && AXIsProcessTrusted()` 通过后，激活目标 App → `waitForFrontmost` 轮询（最多 20 次 × 30ms）等目标成为 frontmost → `sendCommandV()` 用 `CGEvent`（`.combinedSessionState` 源，投递到 `.cghidEventTap`）合成 ⌘V 按下/抬起。
  - Accessibility 未授权时：已复制但不激活目标（函数提前 return）。
- `ensureAccessibility(prompt:)`：`AXIsProcessTrustedWithOptions` 触发系统授权弹窗。

## 5. Global hotkeys（两层）

**系统级全局热键** — `Hotkey/HotKeyCenter.swift`（Carbon，仅 1 个可配置）：
- `RegisterEventHotKey`（默认 ⌘⇧V，即 keyCode `kVK_ANSI_V` + `cmdKey|shiftKey`），signature `0x50535459`（'PSTY'）id=1，经 `InstallEventHandler(kEventHotKeyPressed)` 回调派发到主线程 `onTrigger` → `toggleBar`。
- `reload()` 防丢失设计（针对 issue #64）：先解绑旧的再注册新的；注册失败则**恢复旧注册**并 1s 间隔重试 5 次——宁可保留旧组合也不进入"无热键"状态。睡眠唤醒后强制 reload；显示器参数变化**不** reload（防显示 churn 期间误伤）。
- 设置变更（keyCode/modifiers didSet）自动触发 reload。

**面板内按键** — `AppController.handleKey`（local event monitor）+ `BarPanel.performKeyEquivalent`：
- ⌘⇧S → Settings；⌘⇧P → 暂停/恢复（走 `performKeyEquivalent` 前置拦截，保证 SwiftUI 之前处理）。
- 方向键移动选择；return 粘贴选中；esc 三段式（清多选 → 清搜索 → 关面板）。
- Quick-paste：可配置修饰键（默认 ⌘）+ 数字 1–9 直接粘贴第 N 张；纯文本修饰键（默认 ⇧，与 quick-paste 修饰键互斥，设置层强制不重合）。
- 退格键防误删三重保护：忽略按键重复、搜索清空后 0.6s 冷却、空搜索才删除；⌘⌫/前向删除直接删。
- 无修饰可打印字符追加进 `searchText`（搜索即打字）。

## 6. NSPanel / SwiftUI UI

`UI/BarWindowController.swift`：
- `BarPanel: NSPanel`：borderless、`.floatingPanel`、`.modalPanel` 级（可配）、`canJoinAllSpaces + fullScreenAuxiliary + stationary`、可成为 key window（`canBecomeKey` override）、`isReleasedWhenClosed=false` 防窗口被意外释放。
- 显示状态机 `Phase { hidden / showing(n) / shown / hiding(n) }` + `epoch` 令牌：动画完成 handler 与超时兜底 timer 双路径 `settle`，谁先到谁生效——规避 AppKit 丢弃动画完成回调导致面板"永久可见/再也不出现"的 bug（issue #64）。
- **多显示器安全**：面板 frame 始终停在最终位置，动画是**内容在窗口内部滑动**（旧方案动画窗口 frame，在纵向堆叠显示器上会先整条出现在邻屏）；目标屏取 `NSEvent.mouseLocation` + `NSMouseInRect` 命中，边界点/缺口用最近屏兜底（`NSScreen.main` 是关键窗口所在屏，不可靠）。
- macOS 26+：内容包在 `NSGlassEffectView`（圆角玻璃）里，内容底部预留圆角高度；macOS 14/15：`NSVisualEffectView(.hudWindow)` + 自绘圆角裁剪。

SwiftUI 视图：
- `BarView`：顶栏（MAS 时 CloudKit 同步按钮/非 MAS 时 iCloud Drive 按钮、搜索指示器、`PinboardTabs`、批量删除、更多菜单）+ 横向 `LazyHStack` 卡片条 + 空态。`barPresentationToken` 变化重置滚动；`selectedID` 变化居中滚动。
- `ClipCardView`（310 行）：卡片头（`SourceColor` 按 bundleID 稳定取色 + App 图标 `AppIconProvider` 带缓存 + 类型/相对时间）、按类型的内容区、页脚（字符数/链接/文件数 + quick-paste 序号）、选中描边、点击/双击/⇧点击扩展选/⌘点击多选、`onDrag`（`ClipDragProvider` 按类型注册 NSItemProvider）、完整右键菜单（粘贴/纯文本粘贴/复制/编辑/Writing Tools/重命名/Pin 到 Pinboard/预览/分享/删除）。
- `PinboardTabs`：胶囊标签（Clipboard + 各 Pinboard + 新建），右键重命名/删除；`TextPrompt`（NSAlert + NSTextField 模态输入）。
- `ClipPreviewView`：普通 NSWindow 里的预览（540×400，可复用单例窗口）。
- `ClipEditor`（471 行）：模态 NSPanel + NSTextView 富文本编辑器（B/I/U/S 工具栏、字数统计、⌘↩ 保存、Writing Tools（macOS 15.2+）、颜色 clip 用 NSColorWell 弹窗编辑）。
- `Theme.swift`：集中常量（卡片宽/圆角/配色）+ Date 相对时间格式化。

## 7. Settings

- `Settings.swift`：`@Observable` 单例包装 UserDefaults，`didSet` 即写入（`isLoaded` 防初始化期回写），带 clamp（historyLimit ≥ 20、barHeight 240–720、retentionDays ≥ 1）。部分键有副作用：热键 → `HotKeyCenter.reload()`；launchAtLogin → `SMAppService`；showMenuBarIcon → 状态栏图标增删。`ignoredSourceAppBundleIDs` 是隐私排除列表（bundleID 存储而非路径）。
- `SettingsView.swift`：520×560 窗口三 Tab——General（热键录制、保留策略草稿+确认对话框、quick-paste/纯文本修饰键、行为开关、同步开关、Accessibility 权限区（非 MAS，1s 轮询授权状态）、清除历史）/ Privacy（排除 App 选择器 NSOpenPanel）/ About（版本、MIT、上游 GitHub 链接）。
- `HotkeyRecorderView`：录制态安装 local monitor，必须含 ⌘/⌥/⌃ 之一否则 beep。
- 保留策略修改走"草稿 → 评估删除量 → 确认弹窗 → commit"，防误删。

## 8. iCloud sync（两套互不相通的实现）

**非 MAS（直装版）：iCloud Drive 文件同步**
- `ClipboardStore.setICloudSync(true)`：把存储目录切到 `CloudDocs/Pesty/`，迁移时复制 images、首次读取远端 store.json 后 `mergeExternal` 全量合并（按 contentKey 去重、按 createdAt 排序、Pinboard 按 id 合并成员）。
- `DispatchSourceFileSystemObject`（`.write/.rename/.delete`）监视 store.json；`ignoreWatchUntil`（写后 1.5s）抑制自己的回声；事件到达 → 读文件 → `mergeExternal` → **重新建立 watch**（每次事件后 cancel + 重建）。
- 整文件级同步：无记录级冲突解决，靠"每次全量合并 + contentKey 去重"收敛；并发写的丢失窗口见 RISKS.md R3。

**MAS：CloudKit / CKSyncEngine（`Sync/CloudSyncService.swift`，整个文件 `#if MAS` 包裹）**
- 容器 `iCloud.com.greycorelabs.pesty`，自定义 zone `PestyZone`，私有数据库。
- 数据模型 `CloudKitSchema`（`CloudKitSchema.swift`）：ClipItem ↔ `CKRecord(recordName=UUID)`；>200KB 的 text/rtf 落 `CKAsset`（先写 `ck-tmp` 暂存，发送成功/引擎启动时清理）；图片直接 CKAsset。注释声明此文件是"wire contract"，iOS 端 App 有字节级相同副本（`PestyKit/Sync/CloudKitSchema.swift`，不在本仓库）。
- 同步算法：`desiredRecords()` 生成期望集 → 与 `shadow`（recordName → 上次推送指纹 SHA256）diff → 增删队列交给 `CKSyncEngine`；`pestyStoreDidSave` 通知驱动 diff。远端变更 `applyRemote` 进 store（先过本地隐私排除列表再入历史），冲突策略 v1 = **server wins**（`serverRecordChanged` → 本地重写）；`zoneNotFound` → 重建 zone 全量重传；账号切换 → 清空 shadow/state 并关同步（不跨账号合并）；本地裁剪（retention）通过 `retentionPrunedRecordNames` 不误删云端。
- CKRecord system fields 逐条缓存在 `ck-system-fields/`（NSKeyedArchiver），保证 save 能带上 server change tag。
- 引擎 state serialization 持久化到 `cksync-state.json`。

## 9. 构建与打包

- `Package.swift`：`swift-tools-version:6.0`，macOS 14+，executable `Pesty`，`-parse-as-library` unsafeFlag（`@main struct` 在 Main.swift 中需要），语言模式 v5。
- `scripts/build_app.sh`：`swift build -c release --arch arm64 --arch x86_64` 通用二进制 → 组装 `packaging/Pesty.app`（Info.plist 版本号 sed 替换）→ `make_icon.sh`（`IconGen.swift` 画 1024 png → sips 切尺寸 → iconutil 出 icns）→ **ad-hoc 签名**但带稳定 designated requirement（identifier 固定），保证 Accessibility 授权跨构建不失效。
- `scripts/build_mas.sh`：`-Xswiftc -DMAS` 沙盒构建 + provisioning profile + Apple Distribution 签名 + productbuild 出 pkg（依赖上游作者的证书与 profile，fork 无法复现）。
- `scripts/sign_notarize.sh`：Developer ID + hardened runtime + notarytool 公证 + DMG（脚本里硬编码了上游作者的 ASC key id/issuer 默认值）。
- CI（`.github/workflows/ci.yml`）：macos-15 + Xcode 26.3，`swift build` + `build_app.sh`。Release workflow：手动触发，需要 6 个签名 secrets（上游仓库未配置）。
