# ARCHITECTURE.md — 冻结架构与真实行为边界

基准：`momenbasel/pesty@70fd6b2c47d48ce03932e7e43b930f9ee26fcec7`  
本文描述冻结源码的结构、数据流和失效边界。它不是正确性证明。

## 1. 系统总图

```text
NSPasteboard.general
    ↓ 0.4s polling
ClipboardMonitor
    ↓ ClipItem
ClipboardStore
    ├─ history / pinboards / selection
    ├─ store.json + images/
    ├─ direct-download iCloud Drive whole-file merge
    └─ MAS-only CloudKit / CKSyncEngine
           ↓
BarWindowController + SwiftUI views
           ↓
PasteService
           ↓
NSPasteboard write + target activation + optional synthetic ⌘V
```

运行形态：

- 原生 macOS；
- Swift 6 工具链、Swift language mode 5；
- AppKit + SwiftUI + Carbon；
- SwiftPM 单 executable target；
- 无第三方运行时依赖；
- 菜单栏 accessory app；
- `#if MAS` 分出直装版与 Mac App Store 版。

## 2. 生命周期

`Main.swift` 手工创建 `NSApplication`，由 `AppController` 作为 delegate。

启动路径：

```text
activation policy
→ main menu
→ workspace/screen/wake observers
→ ClipboardMonitor.start
→ HotKeyCenter.start
→ status item / launch-at-login
→ optional sync
→ onboarding or demo
```

关键状态：

- `lastActiveApp`：最近激活且非自身的 App；
- `previousApp`：显示面板前的前台 App；
- `pasteTarget`：从上述状态和当前前台 App 中选择第一个存活且非自身的进程；
- `barController`：浮动 NSPanel 生命周期；
- `keyMonitor`：仅在面板显示期间处理本地按键。

## 3. 剪贴板捕获

`ClipboardMonitor` 每 0.4 秒检查 `NSPasteboard.changeCount`。

过滤：

- `org.nspasteboard.ConcealedType`；
- `org.nspasteboard.TransientType`；
- 可选的 `AutoGeneratedType`；
- 用户配置的 ignored bundle IDs。

类型优先级：

```text
file URLs
→ png/tiff image
→ rtf/text/link
→ color
```

来源归属优先使用经格式校验的 `org.nspasteboard.source`，否则使用前台 App / `lastActiveApp`。

当前边界：

- 过滤依赖应用是否正确写入约定类型；
- 没有内容级敏感信息识别；
- 文本、RTF 和图片没有单条尺寸上限；
- 轮询和转换在主线程 actor 语境内执行；
- 超大 RTF、TIFF 转 PNG 和全量保存可能造成明显延迟。

## 4. 状态与内容身份

`ClipboardStore` 同时承担：

- history / pinboards；
- 搜索与选择；
- 保留策略；
- 图片文件生命周期；
- JSON 持久化；
- iCloud Drive 整文件同步；
- CloudKit apply 接口。

### 两套不一致的身份函数

捕获去重使用：

```text
ClipItem.sameContent
```

它要求 `type` 相同，然后按类型比较内容。

编辑、外部合并和部分同步路径使用：

```text
contentKey
```

冻结实现把 `.text`、`.richText`、`.link` 都压成 `txt:<text>`。

因此存在：

```text
sameContent(a, b) == false
contentKey(a) == contentKey(b)
```

这不是一个统一不变量。后续修复必须明确选择：

```text
identity = type + normalized content
```

或其他被测试证明的单一定义。

## 5. 持久化

布局：

```text
~/Library/Application Support/Pesty/
  store.json
  images/*.png
  ck-tmp/                  # MAS
  cksync-state.json        # MAS
  cksync-shadow.json       # MAS
  ck-system-fields/        # MAS
```

直装版开启 iCloud Drive 后，根目录切换到：

```text
~/Library/Mobile Documents/com~apple~CloudDocs/Pesty/
```

保存模型：

```text
内存 Snapshot
→ JSONEncoder
→ atomic write
→ chmod 0600
→ post pestyStoreDidSave
```

真实边界：

- `try?` 吞掉编码、写入和 chmod 错误；
- 保存通知当前不代表磁盘写入成功；
- 解码失败静默返回空状态；
- 后续保存可能覆盖原损坏文件；
- 整份历史每次编码与写入；
- 文件监听基于目标文件本身，目标缺失时可能无法重新建立。

## 6. 粘贴链

`PasteService.copy` 按类型重建 pasteboard：

- image：从 `images/` 加载；
- file：URL objects + 文本；
- rich text：RTF + plain text；
- color：NSColor + hex；
- text/link：plain string。

直装版直接粘贴：

```text
copy
→ suppress monitor self-capture
→ optional sound
→ AX trust check
→ activate target
→ wait up to ~600ms for frontmost
→ synthesize ⌘V
```

MAS 版只复制并激活目标 App。

已知关键失效：

- 图片文件缺失时，`copy` 当前返回旧 changeCount；
- `paste` 仍可能继续激活目标并发送 `⌘V`；
- 结果可能是粘贴先前的剪贴板内容；
- target 未在等待预算内成为 frontmost 时，无成功确认或错误反馈；
- Secure Input 可吞掉合成事件。

## 7. 全局热键

`HotKeyCenter` 通过 Carbon 注册一个系统级热键。

冻结实现的意图：

```text
注销旧热键
→ 注册新热键
→ 失败时恢复旧热键
→ 延迟重试
```

冻结实现的真实问题：

1. `unregister()` 后保存的旧 `EventHotKeyRef` 已失效；重新赋值不等于重新注册。
2. `keyCode == 0` 被当成禁用，但 Carbon 的 `kVK_ANSI_A` 合法值就是 `0`。
3. Hotkey recorder 允许录入 A，因此可确定性触发“注销后不再注册”。
4. 延迟重试没有 generation/cancellation token，旧重试可能注销后来注册成功的新组合。
5. 当前测试仅覆盖显示字符串，不覆盖 Carbon 注册状态机。

因此，本文不宣称“失败后必然保留旧热键”。

## 8. UI

主界面是底部浮动 `NSPanel`，内容由 SwiftUI 承载。

包含：

- 横向卡片；
- source app 颜色与图标；
- 搜索；
- Pinboards；
- quick paste；
- 多选与删除；
- 编辑、预览、分享、拖拽；
- macOS 26 glass / 旧系统 visual effect 分支。

`BarWindowController` 有动画 epoch 和 settle 兜底，用于避免动画完成回调丢失。多屏定位逻辑具有历史 issue 背景，但本基准没有真实多显示器证明；重构时应视为保护区。

## 9. 同步

### 直装版：iCloud Drive 整文件合并

行为：

```text
共享 store.json 发生变化
→ 读取 Snapshot
→ 本地 + 外部取并集
→ contentKey 去重
→ 按时间排序
→ 保存
```

这不是完整的分布式同步协议：

- 无 tombstone；
- 无 per-record version；
- 无 causal order；
- 删除和 clear 不能可靠传播；
- 旧设备可把已删除条目重新写回；
- Pinboard metadata 没有确定性冲突选择；
- watch 可能永久丢失；
- whole-file 并发写有覆盖和冲突副本风险。

因此只能描述为“best-effort union merge”，不能描述为可靠删除收敛。

### MAS：CloudKit / CKSyncEngine

具备 record 级模型、shadow fingerprints、zone、system fields 和服务器冲突处理。但 fork 当前不拥有原 team、container、profile 或签名资产，不能把上游 CloudKit 路线视为可发布能力。

## 10. 构建与验证

构建入口：

```text
swift build
swift test
scripts/build_app.sh
scripts/build_mas.sh
scripts/sign_notarize.sh
```

本 fork 的基准入口：

```text
scripts/run_baseline_ci.sh
```

CI 必须对精确 head 运行完整入口。上游 base 的成功状态只能证明上游 base，不证明本 PR 新增 target、脚本或文档。

## 11. 当前发布边界

在独立 remediation PR 完成前：

- 不发布自有品牌版本；
- 不把 iCloud Drive 删除同步标为可靠；
- 不把热键恢复标为可靠；
- 不把本机叙述性 smoke 当作自动回归门；
- 不添加 AI、账户、遥测或网络上传来掩盖基础可靠性问题。
